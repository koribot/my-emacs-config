
- configs
 - dirs: manage display and saving of recent/popular directories and files
    - `key-binding`
       - `C-c r`: recent directories
       - `C-c p`: popular directories
 - functions: callable function in `M-x` and hooks
 - keybindings: custom keybindings
 - language-mode: related to language-modes lsp, etc
 - completion: completion frame work is here
      